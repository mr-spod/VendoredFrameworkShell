use_frameworks!

target 'VendoredFrameworkShell_Example' do
  pod 'VendoredFrameworkShell', :path => '../'

  target 'VendoredFrameworkShell_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 1.2.0'
    pod 'Nimble', '~> 7.0.2'
  end
end
